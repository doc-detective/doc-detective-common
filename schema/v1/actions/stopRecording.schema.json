{
  "title": "stopRecording",
  "type": "object",
  "description": "Stop recording the current browser viewport.",
  "properties": {
    "action": {
      "type": "string",
      "const": "stopRecording"
    }
  },
  "required": [
    "action"
  ],
  "additionalProperties": false
}